services:
  otel-collector:
    image: otel/opentelemetry-collector:0.116.1
    container_name: bun-testing-otel-collector
    command: ["--config=/etc/otelcol/config.yaml"]
    volumes:
      - ./infra/otel/collector.yaml:/etc/otelcol/config.yaml:ro
    ports:
      - "4318:4318"

  prometheus: