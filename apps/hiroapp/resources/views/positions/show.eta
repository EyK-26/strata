<div class="PositionDetail">
  <h2><%= it.position.name %></h2>
  <p><%= it.position.description %></p>
  <p>Department: <%= it.position.department?.name %></p>
  <p>Grade: <%= it.position.grade?.name %></p>
  <% if (it.currentUser?.role_id !== 2 && it.position.hiring) { %>
    <form action="/positions/<%= it.position.id %>/close" method="post">
      <%~ include("../partials/csrf.eta") %>
      <button type="submit">Stop hiring</button>
    </form>
  <% } %>
  <% if (it.currentUser?.role_id !== 2 && !it.position.hiring && !it.position.user_id) { %>
    <form action="/positions/<%= it.position.id %>/reopen" method="post">
      <%~ include("../partials/csrf.eta") %>
      <button type="submit">Reopen hiring</button>
    </form>
  <% } %>
  <% if (it.currentUser?.role_id === 1) { %>
    <form action="/positions/<%= it.position.id %>/delete" method="post">
      <%~ include("../partials/csrf.eta") %>
      <button type="submit">Delete position</button>
    </form>
  <% } %>
  <% if (it.currentUser?.role_id !== 2) { %>
    <p><a href="/positions/<%= it.position.id %>/skills">Skills and match</a></p>
    <% if (it.position.department_id) { %>
      <p><a href="/departments/<%= it.position.department_id %>/applications">Department applications</a></p>
    <% } %>
    <form action="/positions/<%= it.position.id %>/interviewers" method="post">
      <%~ include("../partials/csrf.eta") %>
      <label>Interviewer user ids <input type="text" name="user_ids" placeholder="3,1" /></label>
      <button type="submit">Assign interviewers</button>
    </form>
  <% } %>
  <form action="/positions/<%= it.position.id %>/watch" method="post">
    <%~ include("../partials/csrf.eta") %>
    <button type="submit">Toggle watch</button>
  </form>
  <% if (it.currentUser?.role_id !== 2 && it.career && it.career.status === "scheduled") { %>
    <p>Scheduled for careers · <%= it.career.publish_at %></p>
    <form action="/careers/<%= it.career.id %>/unpublish" method="post">
      <%~ include("../partials/csrf.eta") %>
      <input type="hidden" name="return_to" value="/positions/<%= it.position.id %>" />
      <button type="submit">Cancel scheduled publish</button>
    </form>
  <% } %>
  <% if (it.currentUser?.role_id !== 2 && it.career && it.career.status === "published") { %>
    <p>Published on careers<% if (it.career.expires_at) { %> · expires <%= it.career.expires_at %><% } %></p>
    <form action="/careers/<%= it.career.id %>/unpublish" method="post">
      <%~ include("../partials/csrf.eta") %>
      <input type="hidden" name="return_to" value="/positions/<%= it.position.id %>" />
      <button type="submit">Unpublish from careers</button>
    </form>
    <form action="/careers/<%= it.career.id %>/expire" method="post">
      <%~ include("../partials/csrf.eta") %>
      <input type="hidden" name="return_to" value="/positions/<%= it.position.id %>" />
      <button type="submit">Expire from careers</button>
    </form>
    <% if (it.career.pinned) { %>
      <form action="/careers/<%= it.career.id %>/unpin" method="post">
        <%~ include("../partials/csrf.eta") %>
        <input type="hidden" name="return_to" value="/positions/<%= it.position.id %>" />
        <button type="submit">Unpin career</button>
      </form>
    <% } else { %>
      <form action="/careers/<%= it.career.id %>/pin" method="post">
        <%~ include("../partials/csrf.eta") %>
        <input type="hidden" name="return_to" value="/positions/<%= it.position.id %>" />
        <button type="submit">Pin career</button>
      </form>
    <% } %>
  <% } %>
  <% if (it.currentUser?.role_id !== 2 && it.position.hiring && (!it.career || it.career.status === "unpublished" || it.career.status === "expired")) { %>
    <form class="CareerForm" action="/positions/<%= it.position.id %>/career" method="post">
      <%~ include("../partials/csrf.eta") %>
      <input type="hidden" name="return_to" value="/positions/<%= it.position.id %>" />
      <label>Publish at <input type="datetime-local" name="publish_at" /></label>
      <label>Expires <input type="datetime-local" name="expires_at" /></label>
      <button type="submit">Publish to careers</button>
    </form>
  <% } %>
  <h3>Applicants</h3>
  <% it.applications.forEach((application) => { %>
    <a class="stack-row" href="/applications/<%= application.id %>">
      <span><%= application.user?.first_name %> <%= application.user?.last_name %></span>
      <span><%= application.status?.name %></span>
    </a>
  <% }) %>
  <% if (it.currentUser?.role_id !== 2) { %>
    <h3>Requisition</h3>
    <% if (it.requisition) { %>
      <p><%= it.requisition.status %> · <%= it.requisition.notes %></p>
      <% if (it.currentUser?.role_id === 1 && it.requisition.status === "submitted") { %>
        <form action="/requisitions/<%= it.requisition.id %>/approve" method="post">
          <%~ include("../partials/csrf.eta") %>
          <input type="hidden" name="return_to" value="/positions/<%= it.position.id %>" />
          <button type="submit">Approve requisition</button>
        </form>
        <form action="/requisitions/<%= it.requisition.id %>/reject" method="post">
          <%~ include("../partials/csrf.eta") %>
          <input type="hidden" name="return_to" value="/positions/<%= it.position.id %>" />
          <label>Notes <textarea name="notes"></textarea></label>
          <button type="submit">Reject requisition</button>
        </form>
      <% } %>
    <% } %>
    <% if (!it.requisition || it.requisition.status === "rejected") { %>
      <form class="RequisitionForm" action="/positions/<%= it.position.id %>/requisition" method="post">
        <%~ include("../partials/csrf.eta") %>
        <input type="hidden" name="return_to" value="/positions/<%= it.position.id %>" />
        <label>Notes <textarea name="notes"></textarea></label>
        <button type="submit">Submit requisition</button>
      </form>
    <% } %>
  <% } %>
  <% if (it.currentUser?.role_id !== 2) { %>
    <h3>Referrals</h3>
    <% (it.referrals ?? []).forEach((referral) => { %>
      <p><%= referral.name %> · <%= referral.email %> · <%= referral.status %></p>
      <% if (referral.status !== "closed") { %>
        <form action="/referrals/<%= referral.id %>/close" method="post">
          <%~ include("../partials/csrf.eta") %>
          <input type="hidden" name="return_to" value="/positions/<%= it.position.id %>" />
          <button type="submit">Close referral</button>
        </form>
      <% } %>
    <% }) %>
    <form class="ReferralForm" action="/positions/<%= it.position.id %>/referrals" method="post">
      <%~ include("../partials/csrf.eta") %>
      <input type="hidden" name="return_to" value="/positions/<%= it.position.id %>" />
      <label>Name <input type="text" name="name" /></label>
      <label>Email <input type="email" name="email" /></label>
      <label>Notes <textarea name="notes"></textarea></label>
      <button type="submit">Add referral</button>
    </form>
  <% } %>
  <h3>Interview slots</h3>
  <% (it.slots ?? []).forEach((slot) => { %>
    <p><%= slot.starts_at %> – <%= slot.ends_at %> · <%= slot.status %></p>
    <% if (it.currentUser?.role_id !== 2 && slot.status === "open") { %>
      <form action="/slots/<%= slot.id %>/cancel" method="post">
        <%~ include("../partials/csrf.eta") %>
        <input type="hidden" name="return_to" value="/positions/<%= it.position.id %>" />
        <button type="submit">Cancel slot</button>
      </form>
    <% } %>
    <% if (it.currentUser?.role_id === 2 && slot.status === "open") { %>
      <form action="/slots/<%= slot.id %>/book" method="post">
        <%~ include("../partials/csrf.eta") %>
        <input type="hidden" name="return_to" value="/positions/<%= it.position.id %>" />
        <button type="submit">Book slot</button>
      </form>
    <% } %>
  <% }) %>
  <% if (it.currentUser?.role_id !== 2) { %>
    <form class="SlotForm" action="/positions/<%= it.position.id %>/slots" method="post">
      <%~ include("../partials/csrf.eta") %>
      <input type="hidden" name="return_to" value="/positions/<%= it.position.id %>" />
      <label>Starts <input type="datetime-local" name="starts_at" /></label>
      <label>Ends <input type="datetime-local" name="ends_at" /></label>
      <button type="submit">Add slot</button>
    </form>
  <% } %>
  <div class="comments">
    <h3>Comments</h3>
    <% (it.comments ?? []).forEach((comment) => { %>
      <p><%= comment.author ? (comment.author.first_name + " " + comment.author.last_name) : "Staff" %> · <%= comment.body %></p>
    <% }) %>
    <form action="/positions/<%= it.position.id %>/comments" method="post">
      <%~ include("../partials/csrf.eta") %>
      <label>Add comment <textarea name="body"></textarea></label>
      <button type="submit">Save comment</button>
    </form>
  </div>
</div>
