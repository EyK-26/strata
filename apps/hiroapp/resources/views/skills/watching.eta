<div class="Watchlist">
  <h2>Watchlist</h2>
  <p>Inbox alerts when a watched seat is published to careers.</p>
  <% it.positions.forEach((position) => { %>
    <a class="stack-row" href="/positions/<%= position.id %>">
      <span><%= position.name %></span>
    </a>
  <% }) %>
  <% if (it.positions.length === 0) { %>
    <p>You are not watching any positions.</p>
  <% } %>
</div>
