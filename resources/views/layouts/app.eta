<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= it.title ?? it.appName ?? "WorkHub" %></title>
    <meta name="csrf-token" content="<%= it.csrfToken ?? "" %>" />
    <meta name="htmx-config" content='{"inlineStyleNonce":"<%= it.cspNonce ?? "" %>"}' />
    <script src="https://unpkg.com/htmx.org@2.0.4"></script>
    <script nonce="<%= it.cspNonce ?? "" %>">
      document.addEventListener("htmx:configRequest", (event) => {
        const token = document.querySelector('meta[name="csrf-token"]')?.getAttribute("content");
        if (token) {
          event.detail.headers["X-CSRF-Token"] = token;
        }
      });
    </script>
    <link rel="stylesheet" href="/assets/app.css" />
  </head>
  <body>
    <header class="site-header">
      <a href="/organizations" class="brand"><%= it.appName ?? "WorkHub" %></a>
      <nav>
        <% if (it.authUser && it.organizations && it.organizations.length > 0) { %>
        <form method="post" action="/current-organization" class="nav-org">
          <input type="hidden" name="_token" value="<%= it.csrfToken ?? "" %>" />
          <label class="nav-org-label" for="current-organization">Team</label>
          <select id="current-organization" name="organization_id">
            <% for (const org of it.organizations) { %>
            <option
              value="<%= org.id %>"
              <%= it.currentOrganization && it.currentOrganization.id === org.id ? "selected" : "" %>
            ><%= org.name %></option>
            <% } %>
          </select>
          <button type="submit">Switch</button>
        </form>
        <% } %>
        <a href="/organizations">Organizations</a>
        <a href="/projects">Projects</a>
        <a href="/tasks">Tasks</a>
        <a href="/search">Search</a>
        <a href="/reports">Reports</a>
        <a href="/billing">Billing</a>
        <a href="/webhooks">Webhooks</a>
        <a href="/admin">Admin</a>
        <% if (it.authUser) { %>
        <div
          id="notifications-bell"
          class="notifications-bell"
          hx-get="/notifications"
          hx-trigger="load, every 30s"
          hx-swap="innerHTML"
        ></div>
        <a href="/account">Account</a>
        <span class="nav-user"><%= it.authUser.email %></span>
        <form method="post" action="/logout" class="nav-logout">
          <input type="hidden" name="_token" value="<%= it.csrfToken ?? "" %>" />
          <button type="submit">Sign out</button>
        </form>
        <% } else { %>
        <a href="/login">Sign in</a>
        <% } %>
        <a href="/api/v1/reports/summary">API summary</a>
      </nav>
    </header>
    <main class="site-main">
      <% if (it.flash) { %>
      <div class="flash flash-<%= it.flash.level %>" role="status"><%= it.flash.message %></div>
      <% } %>
      <%~ it.body %>
    </main>
  </body>
</html>
